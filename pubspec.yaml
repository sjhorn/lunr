name: lunr
description: A bit like Solr, but much smaller and not as bright (dart port of lunrjs.com)
version: 2.3.11
repository: https://github.com/sjhorn/lunr

environment:
  sdk: ">=3.5.3 <4.0.0"

dependencies:
  collection: ^1.18.0
  intl: ^0.19.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
  coverage: ^1.6.3

